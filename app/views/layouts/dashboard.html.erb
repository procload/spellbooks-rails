<!DOCTYPE html>
<html>
<head>
  <%= render "shared/head" %>
</head>

<body class="bg-background text-foreground">
  <%= render "shared/header", show_back_button: false %>

  <main class="container mx-auto mt-28 px-5 max-w-7xl">
    <%= yield %>
  </main>

  <%= render 'shared/flash_messages' %>
  <div id="toasts" class="fixed bottom-4 right-4 z-50"></div>
</body>
</html>
