<!DOCTYPE html>
<html>
<head>
<title><%= content_for(:title) || "Spellbooks Rails Redux" %></title>
<meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="mobile-web-app-capable" content="yes">
<%= csrf_meta_tags %>
<%= csp_meta_tag %>

<%= yield :head %>

<!-- Add Fira Sans font -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fira+Sans:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

<link rel="icon" href="/icon.png" type="image/png">
<link rel="icon" href="/icon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/icon.png">

<%# Includes all stylesheet files in app/assets/stylesheets %>
<%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
<%= stylesheet_link_tag "application.tailwind", "data-turbo-track": "reload" %>
<%= javascript_importmap_tags %>
</head>

  <body class="bg-background text-foreground">
    <!-- Update header with new styling -->
    <header 
      data-controller="header"
      class="bg-header-bg fixed top-0 left-0 right-0 z-10 shadow-header transform transition-all duration-300 ease-in-out">
      <nav class="container mx-auto px-5 transition-all duration-300 ease-in-out py-4 header-nav">
        <div class="flex items-center justify-center">
          <%= link_to root_path, class: "flex items-center text-primary hover:text-primary/80 transition-all duration-300 ease-in-out", "aria-label": "Spellbooks Rails Redux - Home" do %>
            <%= render "shared/logo", class: "w-12 h-12 transition-all duration-300 ease-in-out" %>
            <span class="sr-only">Spellbooks Rails Redux</span>
          <% end %>
        </div>
      </nav>
    </header>

    <!-- Update main content with new styling -->
    <main class="container mx-auto mt-28 px-5 max-w-assignment">
      <%= yield %>
    </main>

    <!-- Toast container with new styling -->
    <div id="toasts" class="fixed bottom-4 right-4 z-50">
    </div>

    <!-- Update flash messages with new styling -->
    <% flash.each do |name, msg| %>
      <div class="flash <%= name %> fixed top-4 right-4 p-4 rounded-md bg-card text-card-foreground shadow-md" role="alert">
        <%= msg %>
      </div>
    <% end %>

    <%= turbo_stream_from "assignments" %>
  </body>
</html>
