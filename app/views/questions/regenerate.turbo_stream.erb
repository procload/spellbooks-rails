<%= turbo_stream.replace dom_id(@question) do %>
  <%= render "questions/question", 
      question: @question, 
      question_counter: params[:question_counter].to_i,
      assignment: @assignment %>
<% end %> 